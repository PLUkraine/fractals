#ifndef QIMAGEPROXY_H
#define QIMAGEPROXY_H
#include <QImage>
#include <QLineF>
#include <QPainter>
#include <mutex>

class QImageProxy : public QImage
{
public:
    QImageProxy(QSize s);
    void resetImage(QSize s);
    void paintOnImage(QLineF line);
    void paintImage(QPainter painter);
    ~QImageProxy();
private:
    QImage _image;
    std::mutex _imageMutex;
};

#endif // QIMAGEPROXY_H
